<?xml version="1.0" encoding="UTF-8"?>
<phpunit
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/9.6/phpunit.xsd"
	bootstrap="tests/bootstrap.php"
	colors="true"
	convertDeprecationsToExceptions="false"
>
	<testsuites>
		<!-- No FrontAccounting, no database, no web server. -->
		<testsuite name="unit">
			<directory>tests/Unit</directory>
		</testsuite>
		<!-- Anorm against a real database; needs FA_DB_HOST (the docker stack sets it). -->
		<testsuite name="db">
			<directory>tests/Db</directory>
		</testsuite>
		<!-- Drives Apache over HTTP; needs FA_URL (the docker stack sets it). -->
		<testsuite name="http">
			<directory>tests/Http</directory>
		</testsuite>
	</testsuites>
</phpunit>
